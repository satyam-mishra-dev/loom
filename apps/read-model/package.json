{
  "name": "@loom/read-model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "test": "vitest run --exclude '**/*.integration.test.ts' --passWithNoTests",
    "test:integration": "vitest run read-model.integration"
  },
  "dependencies": {
    "@loom/core": "*",
    "@loom/db": "*",
    "@loom/matcher": "*",
    "fastify": "^5.0.0",
    "h3-js": "^4.1.0",
    "ioredis": "^5.4.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^10.13.0",
    "@testcontainers/redis": "^10.13.0",
    "@types/pg": "^8.11.0",
    "vitest": "^3.0.0"
  }
}
